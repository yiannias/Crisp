# Capture overlay label clipped off-screen

When a region capture is started from the tray menu, the overlay's “Select a region” hint is positioned almost entirely beyond the right edge of the active display. The same test also used an older build, so the Editor Escape text did not include the latest label change.
