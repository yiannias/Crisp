# Tray menu frame frozen into region capture

After choosing “Select a region” from Crisp's notification-area menu, the menu's highlighted row and shortcut text are still present in the frozen desktop used by the selection overlay. The notification-area overflow is visible too. The tray command needs a short asynchronous settle period before capturing.
